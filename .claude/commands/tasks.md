---
description: Show the task map and what is done so far
---

Read @docs/tasks/00-overview.md, then check the repository for what actually exists.

Report a table: task number, name, status (done / partial / not started), and the evidence you
used (files present, tests passing). Do not trust ticked checkboxes alone — verify against code.

End with the single next command to run.
